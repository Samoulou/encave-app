# ENC-096 — Page détail événement encaveur (liste des inscrits par session + actions)

## Objectif métier
Offrir à l'encaveur une vue opérationnelle complète d'un événement publié : qui est inscrit, sur quelle session, et quelles actions jour-J sont possibles (check-in, no-show, contact). C'est l'écran central du jour-J et le hub d'où partent ENC-100/102/103b.

## Acteurs
- **WINEMAKER** (acteur principal) : consulte sa page, déclenche les actions opérationnelles.
- **CLIENT** : présent en lecture indirecte (nom, +1, statut booking).
- **ADMIN** : pas concerné ici (admin a sa propre vue, hors-scope).

## Préconditions & déclencheurs
- L'encaveur est authentifié (`await auth()`) et propriétaire de la winery rattachée à l'événement (`winery.ownerId === session.user.id`).
- L'événement existe et n'est pas `ARCHIVED` (mais peut être `DRAFT` ou `PUBLISHED`).
- Déclencheur : clic depuis le dashboard ou l'agenda sur un événement, ou accès direct par URL.

## User stories
- En tant qu'**encaveur**, je veux voir tous les clients inscrits à mon événement, regroupés par session (date + heure), afin de préparer l'accueil du jour-J.
- En tant qu'**encaveur**, je veux pouvoir lancer un scan QR, marquer un client check-in manuellement ou no-show depuis cette page, afin de gérer la présence sans changer d'écran.
- En tant qu'**encaveur**, je veux voir d'un coup d'œil le remplissage de chaque session (X/Y places), afin de savoir où j'en suis commercialement.

## Critères d'acceptation (Gherkin)

```gherkin
Scénario : encaveur consulte un événement publié avec sessions à venir
  Étant donné que je suis connecté comme encaveur propriétaire de la winery "Domaine X"
  Et que mon événement "Dégustation verticale" a 3 sessions à venir
  Quand j'accède à /dashboard/events/[experienceSlug]
  Alors je vois le titre de l'événement, son statut, sa capacité
  Et je vois les 3 sessions listées par ordre chronologique croissant
  Et chaque session affiche : date, heure, places confirmées / capacité totale
  Et chaque session liste les bookings CONFIRMED, COMPLETED et NO_SHOW
```

```gherkin
Scénario : session sans inscrit
  Étant donné une session sans aucun booking
  Quand je consulte la page détail
  Alors la session affiche "0 / 12 places" et un EmptyState "Aucune réservation pour cette session"
```

```gherkin
Scénario : encaveur tente d'accéder à un événement qui ne lui appartient pas
  Étant donné que je suis connecté comme encaveur propriétaire de la winery A
  Quand j'accède à un événement appartenant à la winery B
  Alors je suis redirigé vers /dashboard avec un toast d'erreur "Vous n'avez pas accès à cet événement"
```

```gherkin
Scénario : actions jour-J disponibles uniquement aux sessions du jour
  Étant donné une session prévue dans 3 jours
  Quand je consulte la page
  Alors les boutons "Scanner les QR codes" et "Marquer présent" sont désactivés (tooltip "Disponible le jour de l'événement")
  Et le bouton "Marquer no-show" est désactivé jusqu'à la fin de la session
```

```gherkin
Scénario : session passée
  Étant donné une session terminée la veille
  Quand je consulte la page
  Alors la session apparaît dans une section "Sessions passées" repliée par défaut
  Et chaque booking affiche son statut final (COMPLETED, NO_SHOW, CANCELLED_BY_CLIENT, CANCELLED_BY_WINERY)
  Et aucune action de check-in n'est proposée
```

## Règles métier
- **Tri sessions** : à venir d'abord (chrono croissant), puis "Aujourd'hui" mis en exergue, puis passées (repliées).
- **Comptage capacité** : `placesConfirmées = bookings où status ∈ {CONFIRMED, COMPLETED}`. NO_SHOW reste compté dans confirmé (la place a été vendue). CANCELLED_* ne sont pas comptés.
- **Affichage des bookings** : par session, tri par `createdAt` croissant (premier inscrit en haut).
- **Données par ligne booking** : nom client, email (cliquable mailto), nombre de personnes (`partySize`), référence `ENC-XXXXXX`, statut, heure de check-in si présente.
- **Actions disponibles par booking** :
  - `CONFIRMED` & session du jour : "Marquer présent" (manuel, ENC-102) + "Marquer no-show" (ENC-103b)
  - `COMPLETED` : badge "Présent à HH:MM" + bouton "Annuler le check-in" (revient à CONFIRMED, voir ENC-101 questions ouvertes)
  - `NO_SHOW` : badge no-show + bouton "Annuler le no-show" (revient à CONFIRMED)
  - `CANCELLED_*` : ligne grisée, pas d'action
- **Bouton scan QR** : visible uniquement sur sessions du jour ou à venir dans les 2h (anti-erreur). Renvoie vers `/dashboard/scan?experienceSlug=...&sessionId=...` (ENC-100).
- **Pré-filtre `sessionId`** : si plusieurs sessions le même jour, le scan préselectionne la session si l'encaveur arrive depuis cette session.
- **Accès** : action server `await auth()` + check `winery.ownerId === session.user.id`. Sinon → `redirect('/dashboard')`.

## Copy FR définitive

| Élément | Clé i18n suggérée | Texte FR |
|---|---|---|
| Titre page (h1) | `Dashboard.eventDetail.title` | Détail de l'événement |
| Breadcrumb | `Dashboard.eventDetail.breadcrumb` | Dashboard / Événements / {title} |
| Badge statut DRAFT | `Dashboard.eventDetail.status.draft` | Brouillon |
| Badge statut PUBLISHED | `Dashboard.eventDetail.status.published` | Publié |
| Badge statut ARCHIVED | `Dashboard.eventDetail.status.archived` | Archivé |
| Section sessions à venir | `Dashboard.eventDetail.sections.upcoming` | Sessions à venir |
| Section sessions aujourd'hui | `Dashboard.eventDetail.sections.today` | Aujourd'hui |
| Section sessions passées | `Dashboard.eventDetail.sections.past` | Sessions passées |
| Compteur places | `Dashboard.eventDetail.session.capacity` | {confirmed} / {total} places |
| Session vide | `Dashboard.eventDetail.session.empty` | Aucune réservation pour cette session |
| Bouton scan QR | `Dashboard.eventDetail.actions.scanQr` | Scanner les QR codes |
| Tooltip scan désactivé | `Dashboard.eventDetail.actions.scanDisabled` | Disponible le jour de l'événement |
| Bouton contacter inscrits | `Dashboard.eventDetail.actions.contactAll` | Contacter les inscrits |
| Colonne client | `Dashboard.eventDetail.columns.client` | Client |
| Colonne personnes | `Dashboard.eventDetail.columns.partySize` | Personnes |
| Colonne référence | `Dashboard.eventDetail.columns.reference` | Référence |
| Colonne statut | `Dashboard.eventDetail.columns.status` | Statut |
| Colonne check-in | `Dashboard.eventDetail.columns.checkIn` | Check-in |
| Badge CONFIRMED | `Dashboard.eventDetail.bookingStatus.confirmed` | Confirmée |
| Badge COMPLETED | `Dashboard.eventDetail.bookingStatus.completed` | Présent |
| Badge NO_SHOW | `Dashboard.eventDetail.bookingStatus.noShow` | Absent (no-show) |
| Badge CANCELLED_BY_CLIENT | `Dashboard.eventDetail.bookingStatus.cancelledByClient` | Annulée par le client |
| Badge CANCELLED_BY_WINERY | `Dashboard.eventDetail.bookingStatus.cancelledByWinery` | Annulée par vos soins |
| Heure check-in | `Dashboard.eventDetail.checkIn.at` | Présent à {time} |
| Personnes inline | `Dashboard.eventDetail.partySize.inline` | {count, plural, =1 {1 personne} other {# personnes}} |
| Toast accès refusé | `Dashboard.eventDetail.errors.notOwner` | Vous n'avez pas accès à cet événement. |
| Erreur chargement | `Dashboard.eventDetail.errors.loadFailed` | Impossible de charger l'événement. Réessayez dans un instant. |

## États UI
- **Loading** : skeleton page (header + 3 cards session avec lignes grises). Utiliser `loading.tsx` au niveau route.
- **Empty (aucune session)** : `EmptyState` "Vous n'avez pas encore programmé de sessions" + CTA "Ajouter une session" (lien vers édition événement).
- **Empty (session sans inscrit)** : ligne inline "Aucune réservation pour cette session".
- **Error** : `error.tsx` au niveau route avec bouton "Réessayer".
- **Populated** : header événement + sections session triées + tableau bookings par session.

## Cas limites
- **Événement DRAFT** : la page reste accessible mais bannière "Cet événement n'est pas publié. Aucun client ne peut le voir." + scan QR caché.
- **Événement ARCHIVED** : lecture seule, aucune action disponible, bannière "Événement archivé".
- **Session annulée par encaveur (via ENC-045)** : section "Sessions annulées" séparée, lignes en gris, lien vers détail refund.
- **Capacité dépassée (≥ capacité)** : badge "Complet" sur la session.
- **Encaveur sans winery VERIFIED** : ne devrait pas pouvoir y arriver (l'événement n'existe pas). Si edge case : redirect dashboard.
- **Plus de 50 inscrits sur une session** : tableau scrollable verticalement, pagination interne à 50 (lazy load suite si > 50). MVP : on assume ≤ 50, on traite le surplus en P1.
- **Mobile** : tableau passe en cartes empilées (mobile-first), boutons d'action en bottom sheet.

## Dépendances
- **Autres US** :
  - ENC-100 (cible du bouton scan)
  - ENC-101 (alimente la colonne check-in via `checkedInAt`)
  - ENC-102 (action "Marquer présent" manuelle)
  - ENC-103b (action "Marquer no-show")
  - ENC-097 P1 (bouton "Contacter les inscrits" mailto)
  - ENC-045 P0 (sessions annulées affichées ici)
- **Données Prisma** :
  - `Experience` (slug, title, status, capacity, winery)
  - `Booking` (status, partySize, reference, checkedInAt, client, sessionId)
  - `ExperienceSession` (startsAt, endsAt, experienceId, blockedDate)
- **Services externes** : aucun pour cette US (les actions externes sont dans les US suivantes).

## Hors-périmètre explicite
- Édition de l'événement depuis cette page (existe ailleurs, hors-scope).
- Export CSV des inscrits (post-MVP).
- Notifications push à l'encaveur (post-MVP).
- Mailto groupé (ENC-097, P1, séparé).
- Vue mobile dédiée optimisée pour le scan continu (c'est `/dashboard/scan` qui prend le relais).

## Métriques de succès
- 100 % des sessions du jour-J consultées par leur encaveur dans les 24h précédant la session (cible bêta Fondateurs).
- Temps moyen "ouverture page → check-in premier client" < 30 secondes.

## ❓ Questions ouvertes pour Sam
1. **Annuler un check-in déjà fait** : on autorise un retour `COMPLETED → CONFIRMED` ? Proposition par défaut : **oui**, c'est un fix d'erreur opérationnelle, on log l'action mais on autorise la transition arrière exceptionnelle pour ce cas précis. Alternative : interdire et obliger un support ticket.
2. **Sessions du même jour multiples** : on affiche "Aujourd'hui" en haut, mais comment trier 3 sessions à 10h/14h/17h ? Proposition par défaut : tri chrono, mise en exergue de la session "en cours" (entre `startsAt` et `endsAt`) avec badge "En cours".
3. **Période de visibilité du bouton scan** : H-2 avant la première session du jour jusqu'à H+2 après la dernière. Tu valides ? Alternative : toute la journée (00h-23h59 locale).

