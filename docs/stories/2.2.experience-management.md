# Story 2.2: Experience Management Dashboard

## Status

Ready for Review

## PM Validation

| Criteria | Status |
|----------|--------|
| Clear user story | ✅ |
| Numbered ACs | ✅ 12 ACs |
| Technical notes | ✅ Server actions, status colors |
| Files identified | ✅ 5 files/folders |
| Dependencies | ✅ Story 2.1 (Experience model) |
| Effort estimate | Medium (1-2 days) |

**Validated by:** John (PM Agent) on 2026-01-09

## Story

**As a** winemaker,
**I want** to manage my experiences (edit, publish, archive),
**so that** I can keep my offerings up to date.

## Acceptance Criteria

1. Experiences list page (`/dashboard/experiences`) showing all winemaker's experiences
2. List displays: cover photo thumbnail, title, type badge, price, status badge, last updated
3. Status badges color-coded: Draft (gray), Published (green), Archived (amber)
4. "Create New Experience" CTA button prominently displayed
5. Edit action opens edit form (`/dashboard/experiences/[id]/edit`) with pre-populated data
6. "Publish" action available for `DRAFT` experiences, sets status to `PUBLISHED`
7. "Unpublish" action available for `PUBLISHED` experiences, reverts to `DRAFT`
8. "Archive" action available for any status, sets to `ARCHIVED` (soft delete)
9. "Duplicate" action creates a copy in `DRAFT` status
10. Confirmation modal for destructive actions (archive)
11. Empty state with guidance: "Create your first experience to attract visitors"
12. Sort by: newest, oldest, alphabetical, status

## Tasks / Subtasks

- [x] Build experiences list page (AC: 1, 4, 11)
  - [x] Create `/app/(protected)/dashboard/experiences/page.tsx`
  - [x] Add "Create New Experience" CTA button
  - [x] Create empty state component with guidance
- [x] Build experiences table/grid (AC: 2, 3)
  - [x] Create ExperiencesList component
  - [x] Display cover photo thumbnail
  - [x] Display title, type badge, price, status badge, last updated
  - [x] Create status badge component with color coding
- [x] Build edit page (AC: 5)
  - [x] Create `/app/(protected)/dashboard/experiences/[id]/edit/page.tsx`
  - [x] Re-use ExperienceForm with pre-populated data
  - [x] Create `updateExperience` server action
- [x] Implement status actions (AC: 6, 7)
  - [x] Create `publishExperience` server action
  - [x] Create `unpublishExperience` server action
  - [x] Add action buttons with appropriate visibility
- [x] Implement archive action (AC: 8, 10)
  - [x] Create `archiveExperience` server action
  - [x] Build confirmation modal for archive action
- [x] Implement duplicate action (AC: 9)
  - [x] Create `duplicateExperience` server action
  - [x] Copy all fields with status = DRAFT
  - [x] Generate new unique slug
- [x] Implement sorting (AC: 12)
  - [x] Add sort dropdown: newest, oldest, alphabetical, status
  - [x] Use nuqs for URL state

## Dev Notes

### Tech Stack Reference

- **UI:** shadcn/ui Table, Badge, Dialog components
- **State:** nuqs for sort parameter in URL
- **Forms:** React Hook Form for edit form

### Source Tree Reference

```
src/
├── app/
│   └── (protected)/
│       └── dashboard/
│           └── experiences/
│               ├── page.tsx
│               └── [id]/
│                   └── edit/
│                       └── page.tsx
├── components/
│   └── features/
│       └── experience/
│           ├── ExperiencesList.tsx
│           ├── ExperienceForm.tsx
│           ├── StatusBadge.tsx
│           └── ArchiveConfirmModal.tsx
└── server/
    └── actions/
        └── experience.ts
```

### Status Badge Colors

```typescript
const STATUS_COLORS = {
  DRAFT: 'bg-gray-100 text-gray-800',
  PUBLISHED: 'bg-green-100 text-green-800',
  ARCHIVED: 'bg-amber-100 text-amber-800',
};
```

### Server Actions

```typescript
// experience.ts actions
export async function publishExperience(
  id: string
): Promise<ActionResult<Experience>>;
export async function unpublishExperience(
  id: string
): Promise<ActionResult<Experience>>;
export async function archiveExperience(
  id: string
): Promise<ActionResult<Experience>>;
export async function duplicateExperience(
  id: string
): Promise<ActionResult<Experience>>;
export async function updateExperience(
  id: string,
  data: ExperienceInput
): Promise<ActionResult<Experience>>;
```

### Testing

**Test Location:** `tests/unit/`, `tests/integration/`
**Frameworks:** Vitest, React Testing Library

**Test Cases:**

- Publish action only available for DRAFT experiences
- Unpublish action only available for PUBLISHED experiences
- Archive sets status to ARCHIVED
- Duplicate creates copy with DRAFT status and new slug
- Sort options update URL and reorder list
- Empty state shows when no experiences exist

## Change Log

| Date       | Version | Description            | Author   |
| ---------- | ------- | ---------------------- | -------- |
| 2026-01-07 | 1.0     | Initial story creation | PM Agent |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References

- Fixed lint error: Unused parameter `open` in `onOpenChange` type definition in `ArchiveConfirmModal.tsx` - prefixed with underscore

### Completion Notes List

- **All ACs Implemented:**
  - AC1: Experiences list page at `/dashboard/experiences` with full experience list
  - AC2-3: Card-based grid display with cover photo, title, type badge, price, status, last updated
  - AC4: "Create New Experience" CTA button prominently displayed
  - AC5: Edit page at `/dashboard/experiences/[id]/edit` with pre-populated EditExperienceForm
  - AC6: Publish action for DRAFT experiences via dropdown menu
  - AC7: Unpublish action for PUBLISHED experiences via dropdown menu
  - AC8: Archive action available for any status (except already ARCHIVED)
  - AC9: Duplicate action creates copy with DRAFT status and unique slug
  - AC10: Confirmation modal for archive action (ArchiveConfirmModal)
  - AC11: Empty state with guidance message and create CTA
  - AC12: Sort dropdown with nuqs URL state (newest, oldest, alphabetical, status)

- **Validation Results:**
  - Tests: 157 passed (12 new integration tests for experience actions)
  - Lint: Pass (only pre-existing warnings in email.service.ts)
  - Build: Success

### File List

**New Files:**
- `src/components/features/experience/StatusBadge.tsx` - Color-coded status badge component
- `src/components/features/experience/ExperiencesList.tsx` - Experience card grid with action menus
- `src/components/features/experience/ArchiveConfirmModal.tsx` - Confirmation dialog for archive
- `src/components/features/experience/ExperiencesSortSelect.tsx` - Sort dropdown using nuqs
- `src/components/features/experience/EditExperienceForm.tsx` - Pre-populated edit form
- `src/app/(protected)/dashboard/experiences/[id]/edit/page.tsx` - Edit experience page
- `src/components/ui/dropdown-menu.tsx` - shadcn dropdown menu component (added via CLI)
- `tests/integration/actions/experience.test.ts` - Integration tests for experience actions

**Modified Files:**
- `src/app/(protected)/dashboard/experiences/page.tsx` - Added sorting, ExperiencesList component
- `src/server/actions/experience.ts` - Added updateExperience, publishExperience, unpublishExperience, archiveExperience, duplicateExperience, getExperienceForEdit actions

## QA Results

_To be filled after QA review_
