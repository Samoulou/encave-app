# Story 4.1: Winemaker Bookings Dashboard

## Status

Implemented

## Story

**As a** winemaker,
**I want** to see all my reservations in one place,
**so that** I can prepare for upcoming visits and track my activity.

## Acceptance Criteria

1. Bookings dashboard page (`/dashboard/bookings`) as default winemaker landing page
2. Summary cards at top: "Today's Bookings", "This Week", "This Month", "Total Guests"
3. Bookings list with columns: date, time, experience name, client name, guests, status, amount
4. Status badges: Confirmed (green), Completed (blue), Cancelled (gray), No-Show (red)
5. Filter options: by status, by experience, by date range
6. Search by client name or booking reference
7. Sort by: date (default: upcoming first), amount, guest count
8. Click row to expand booking details inline (client contact info, notes)
9. Quick actions: "Mark as Completed", "Mark as No-Show" (for past bookings)
10. "View Client" action shows: email, phone, booking history with winery
11. Export bookings to CSV (filtered results)
12. Empty state for new winemakers: "No bookings yet - share your experiences to get started!"

## Tasks / Subtasks

- [x] Build bookings dashboard page (AC: 1)
  - [x] Create `/app/(protected)/dashboard/bookings/page.tsx`
  - [x] Set as default winemaker landing page
- [x] Build summary cards (AC: 2)
  - [x] Create BookingSummaryCards component
  - [x] Calculate today's bookings
  - [x] Calculate this week's bookings
  - [x] Calculate this month's bookings
  - [x] Calculate total guests
- [x] Build bookings table (AC: 3, 4, 8)
  - [x] Create BookingsTable component
  - [x] Add columns: date, time, experience, client, guests, status, amount
  - [x] Create status badge component with colors
  - [x] Implement row expansion for details
- [x] Implement filters (AC: 5)
  - [x] Add status filter dropdown
  - [x] Add experience filter dropdown
  - [x] Add date range picker
  - [x] Use nuqs for URL state
- [x] Implement search (AC: 6)
  - [x] Add search input
  - [x] Search by client name
  - [x] Search by booking reference
- [x] Implement sorting (AC: 7)
  - [x] Add sort dropdown
  - [x] Default: upcoming first
  - [x] Options: date, amount, guest count
- [x] Implement quick actions (AC: 9)
  - [x] Create `markAsCompleted` server action
  - [x] Create `markAsNoShow` server action
  - [x] Show only for past bookings
- [x] Build client view (AC: 10)
  - [x] Create ClientDetailsModal component
  - [x] Show email, phone
  - [x] Show booking history with winery
- [x] Implement CSV export (AC: 11)
  - [x] Create `exportBookingsCSV` server action
  - [x] Export filtered results
  - [x] Download as file
- [x] Build empty state (AC: 12)
  - [x] Create empty state component
  - [x] Show guidance message

## Dev Notes

### Tech Stack Reference

- **UI:** shadcn/ui Table, Badge, Card components
- **State:** nuqs for filter/sort parameters
- **Export:** CSV generation library

### Source Tree Reference

```
src/
├── app/
│   └── (protected)/
│       └── dashboard/
│           ├── page.tsx           # Redirect to /bookings
│           └── bookings/
│               └── page.tsx
├── components/
│   └── features/
│       └── booking/
│           ├── BookingSummaryCards.tsx
│           ├── BookingsTable.tsx
│           ├── BookingFilters.tsx
│           └── ClientDetailsModal.tsx
└── server/
    ├── actions/
    │   └── booking.ts
    └── queries/
        └── booking.queries.ts
```

### Summary Queries

```typescript
// booking.queries.ts
export async function getBookingSummary(wineryId: string) {
  const today = startOfDay(new Date());
  const weekStart = startOfWeek(today);
  const monthStart = startOfMonth(today);

  const [todayBookings, weekBookings, monthBookings, totalGuests] =
    await Promise.all([
      db.booking.count({
        where: { wineryId, date: today, status: 'CONFIRMED' },
      }),
      db.booking.count({
        where: { wineryId, date: { gte: weekStart }, status: 'CONFIRMED' },
      }),
      db.booking.count({
        where: { wineryId, date: { gte: monthStart }, status: 'CONFIRMED' },
      }),
      db.booking.aggregate({
        where: { wineryId, status: { in: ['CONFIRMED', 'COMPLETED'] } },
        _sum: { guestCount: true },
      }),
    ]);

  return {
    todayBookings,
    weekBookings,
    monthBookings,
    totalGuests: totalGuests._sum.guestCount ?? 0,
  };
}
```

### Status Badge Colors

```typescript
const STATUS_COLORS = {
  CONFIRMED: 'bg-green-100 text-green-800',
  COMPLETED: 'bg-blue-100 text-blue-800',
  CANCELLED_BY_CLIENT: 'bg-gray-100 text-gray-800',
  CANCELLED_BY_WINERY: 'bg-gray-100 text-gray-800',
  NO_SHOW: 'bg-red-100 text-red-800',
};
```

### Testing

**Test Location:** `tests/unit/`, `tests/integration/`
**Frameworks:** Vitest, React Testing Library

**Test Cases:**

- Summary cards calculate correct counts
- Filters narrow results correctly
- Search finds by client name and reference
- Quick actions update booking status
- CSV export contains correct data
- Empty state shows for new winemakers

## Change Log

| Date       | Version | Description            | Author   |
| ---------- | ------- | ---------------------- | -------- |
| 2026-01-07 | 1.0     | Initial story creation | PM Agent |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References

- Fixed TypeScript errors with Prisma types (BookingWhereInput, BookingOrderByWithRelationInput)
- Fixed unused import warnings (Button, useState)
- Replaced use-debounce with custom debounce implementation

### Completion Notes List

- Implemented full bookings dashboard with all 12 acceptance criteria
- Dashboard set as default winemaker landing page via redirect
- Summary cards show today/week/month/total statistics
- Bookings table with expandable rows and responsive design
- Filters: status (multi-select), experience, date range
- Search: by client name or booking reference (debounced)
- Sorting: by date, amount, guest count with URL state persistence
- Quick actions: Mark Completed/No-Show for past CONFIRMED bookings
- Client details modal shows contact info and booking history
- CSV export with filtered results
- Empty state for new winemakers

### File List

**New Files Created:**
- `src/server/queries/booking.queries.ts` - Dashboard queries (getWineryBookings, getBookingSummary, etc.)
- `src/server/actions/booking-dashboard.ts` - Server actions (markBookingCompleted, markBookingNoShow, exportBookingsToCSV, getClientHistory)
- `src/components/features/booking/dashboard/index.ts` - Component exports
- `src/components/features/booking/dashboard/BookingStatusBadge.tsx` - Status badge with colors
- `src/components/features/booking/dashboard/BookingSummaryCards.tsx` - Summary statistics cards
- `src/components/features/booking/dashboard/BookingsEmptyState.tsx` - Empty state component
- `src/components/features/booking/dashboard/BookingFilters.tsx` - Filter controls with nuqs
- `src/components/features/booking/dashboard/BookingSearch.tsx` - Search input with debounce
- `src/components/features/booking/dashboard/BookingsTable.tsx` - Main table with expandable rows
- `src/components/features/booking/dashboard/BookingRowExpanded.tsx` - Expanded row details
- `src/components/features/booking/dashboard/BookingQuickActions.tsx` - Quick action dropdown
- `src/components/features/booking/dashboard/ClientDetailsModal.tsx` - Client history modal
- `src/components/features/booking/dashboard/ExportCSVButton.tsx` - CSV export button
- `src/app/[locale]/(protected)/dashboard/page.tsx` - Redirect to /bookings
- `src/app/[locale]/(protected)/dashboard/bookings/page.tsx` - Main bookings dashboard page

**Modified Files:**
- `src/components/layout/DashboardSidebar.tsx` - Added "Bookings" nav link

## QA Results

### Review Date: 2026-01-12

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Overall: Strong implementation with excellent code organization.**

The implementation demonstrates high-quality TypeScript practices with proper typing, consistent use of the ActionResult pattern, and clean separation of concerns between queries and actions. The component architecture is well-structured with proper barrel exports and appropriate client/server boundaries.

**Strengths:**
- All server actions follow the ActionResult pattern consistently
- Proper authorization checks on all data access paths
- Clean component decomposition (10 focused components)
- Good use of nuqs for URL state management
- Proper error handling with meaningful error codes
- Responsive design with mobile/desktop layouts
- Accessibility considerations (aria labels, screen reader support)

**Code Review Notes:**
- `booking.queries.ts:1-313` - Well-structured query functions with proper typing
- `booking-dashboard.ts:1-355` - Server actions follow auth patterns correctly
- `BookingsTable.tsx:1-253` - Good responsive design implementation

### Refactoring Performed

None - code quality is satisfactory for the current phase.

### Compliance Check

- Coding Standards: ✓ Follows ActionResult pattern, proper typing, no `any` usage
- Project Structure: ✓ Components in correct locations, barrel exports used
- Testing Strategy: ✓ Integration and unit tests added for dashboard functionality (54 tests)
- All ACs Met: ✓ All 12 acceptance criteria implemented

### Requirements Traceability

| AC | Description | Implementation | Test Coverage |
|----|-------------|----------------|---------------|
| 1 | Dashboard as default page | `dashboard/page.tsx` redirect | Indirect (route test) |
| 2 | Summary cards | `BookingSummaryCards.tsx` | `booking.queries.test.ts` |
| 3 | Bookings table columns | `BookingsTable.tsx` | `booking.queries.test.ts` |
| 4 | Status badges | `BookingStatusBadge.tsx` | Component (visual) |
| 5 | Filter options | `BookingFilters.tsx` | `booking.queries.test.ts` |
| 6 | Search functionality | `BookingSearch.tsx` | `booking.queries.test.ts` |
| 7 | Sorting | `BookingsTable.tsx` | `booking.queries.test.ts` |
| 8 | Row expansion | `BookingRowExpanded.tsx` | Component (visual) |
| 9 | Quick actions | `BookingQuickActions.tsx` | `booking-dashboard.test.ts` |
| 10 | View Client modal | `ClientDetailsModal.tsx` | `booking-dashboard.test.ts` |
| 11 | CSV export | `ExportCSVButton.tsx` | `booking-dashboard.test.ts` |
| 12 | Empty state | `BookingsEmptyState.tsx` | Component (visual) |

### Improvements Checklist

- [x] Add integration tests for `markBookingCompleted` action
- [x] Add integration tests for `markBookingNoShow` action
- [x] Add integration tests for `exportBookingsToCSV` action
- [x] Add integration tests for `getClientHistory` action
- [x] Add unit tests for `getWineryBookings` query
- [x] Add unit tests for `getBookingSummary` query
- [ ] Consider implementing pagination for large booking lists
- [ ] Consider caching strategy for summary statistics

### Security Review

**Status: PASS**

- All server actions verify authentication via `auth()`
- Winery ownership verified before any data operations
- Case-insensitive email comparison prevents enumeration attacks
- No exposure of sensitive data in responses
- Status update actions validate booking ownership and business rules

### Performance Considerations

**Status: CONCERNS**

- ✓ `Promise.all` used for parallel data fetching on page load
- ✓ Debounced search input (300ms delay)
- ⚠ No pagination - potential performance issue with many bookings
- ⚠ No caching for summary statistics (recalculated on each load)

**Recommendation:** Consider adding pagination when booking counts exceed reasonable thresholds (e.g., 100+).

### Files Modified During Review

None - no refactoring was performed.

### Gate Status

Gate: PASS → docs/qa/gates/4.1-winemaker-bookings-dashboard.yml

### Recommended Status

✓ Ready for Done

**Rationale:** All 12 acceptance criteria are correctly implemented with excellent code quality. Test coverage has been added with 54 tests covering all server actions and query functions. Remaining unchecked items (pagination, caching) are future enhancements, not blockers.

### Test Files Added

- `tests/integration/actions/booking-dashboard.test.ts` (25 tests)
- `tests/unit/server/queries/booking.queries.test.ts` (29 tests)
