# Story 2.3: Experience Availability Configuration

## Status

Done

## PM Validation

| Criteria | Status |
|----------|--------|
| Clear user story | ✅ |
| Numbered ACs | ✅ 12 ACs |
| Technical notes | ✅ Prisma schema, time slots, validation |
| Files identified | ✅ 5 files/folders |
| Dependencies | ✅ Story 2.1, 2.2 (Experience model & edit page) |
| Effort estimate | Medium-Large (2-3 days) |

**Validated by:** John (PM Agent) on 2026-01-09

## Story

**As a** winemaker,
**I want** to define when my experiences are available,
**so that** visitors know when they can book.

## Acceptance Criteria

1. Prisma schema extended with `AvailabilitySlot` model: id, experienceId, dayOfWeek (0-6), startTime, endTime, isActive
2. Availability configuration section on experience edit page
3. Weekly schedule builder UI: select days of week, add time slots per day
4. Time slot picker: start time (dropdown, 30-min increments from 08:00-20:00), end time (auto-calculated from duration or manual override)
5. Multiple time slots per day supported (e.g., 10:00-12:00 and 14:00-16:00)
6. Toggle to enable/disable specific slots without deleting
7. Visual weekly calendar preview showing configured availability
8. Validation: slots cannot overlap on same day
9. "Copy to all days" helper for common schedules
10. Default empty state: "No availability configured - visitors cannot book yet"
11. Warning shown if experience is `PUBLISHED` but has no availability
12. Changes saved independently from other experience fields

## Tasks / Subtasks

- [x] Extend Prisma schema (AC: 1)
  - [x] Create `AvailabilitySlot` model
  - [x] Add relation to Experience
  - [x] Run migration
- [x] Build availability section (AC: 2, 10)
  - [x] Add availability configuration section to experience edit page
  - [x] Create empty state with guidance message
- [x] Build weekly schedule builder (AC: 3, 4, 5)
  - [x] Create AvailabilityScheduleBuilder component
  - [x] Add day of week selectors (Mon-Sun)
  - [x] Create time slot picker with 30-min increments
  - [x] Auto-calculate end time from experience duration
  - [x] Allow manual end time override
  - [x] Support multiple slots per day
- [x] Implement slot toggle (AC: 6)
  - [x] Add isActive toggle for each slot
  - [x] Visual indication of disabled slots
- [x] Build calendar preview (AC: 7)
  - [x] Create WeeklyCalendarPreview component
  - [x] Show visual representation of availability
- [x] Implement validation (AC: 8)
  - [x] Detect overlapping slots on same day
  - [x] Show validation error for overlaps
- [x] Build "Copy to all days" (AC: 9)
  - [x] Add helper button to copy slots from one day to all days
  - [x] Confirmation before overwriting existing slots
- [x] Add published warning (AC: 11)
  - [x] Show warning if published experience has no availability
- [x] Implement save action (AC: 12)
  - [x] Create `updateAvailability` server action
  - [x] Save independently from other experience fields

## Dev Notes

### Tech Stack Reference

- **UI:** shadcn/ui Switch, Select components
- **Forms:** React Hook Form for slot management
- **Database:** Prisma 5.x

### Source Tree Reference

```
src/
├── app/
│   └── (protected)/
│       └── dashboard/
│           └── experiences/
│               └── [id]/
│                   └── edit/
│                       └── page.tsx
├── components/
│   └── features/
│       └── experience/
│           ├── AvailabilityScheduleBuilder.tsx
│           ├── TimeSlotPicker.tsx
│           └── WeeklyCalendarPreview.tsx
├── server/
│   ├── actions/
│   │   └── availability.ts
│   └── services/
│       └── availability.service.ts
└── lib/
    └── constants/
        └── time-slots.ts
```

### Prisma Schema

```prisma
model AvailabilitySlot {
  id           String     @id @default(cuid())
  experienceId String
  experience   Experience @relation(fields: [experienceId], references: [id], onDelete: Cascade)
  dayOfWeek    Int        // 0 = Sunday, 6 = Saturday
  startTime    String     // "HH:mm" format
  endTime      String     // "HH:mm" format
  isActive     Boolean    @default(true)
  createdAt    DateTime   @default(now())
  updatedAt    DateTime   @updatedAt

  @@index([experienceId, dayOfWeek])
}
```

### Time Slot Generation

```typescript
// Generate 30-minute increments from 08:00 to 20:00
const TIME_SLOTS = Array.from({ length: 25 }, (_, i) => {
  const hour = Math.floor(i / 2) + 8;
  const minute = (i % 2) * 30;
  return `${hour.toString().padStart(2, '0')}:${minute.toString().padStart(2, '0')}`;
});
// ['08:00', '08:30', '09:00', ... '20:00']
```

### Overlap Validation

```typescript
function hasOverlap(slots: Slot[]): boolean {
  const sorted = slots.sort((a, b) => a.startTime.localeCompare(b.startTime));
  for (let i = 1; i < sorted.length; i++) {
    if (sorted[i].startTime < sorted[i - 1].endTime) {
      return true;
    }
  }
  return false;
}
```

### Testing

**Test Location:** `tests/unit/`, `tests/integration/`
**Frameworks:** Vitest, React Testing Library

**Test Cases:**

- Overlap detection catches overlapping slots
- End time auto-calculates from duration
- Copy to all days replicates slots correctly
- Toggle disables slot without deleting
- Published warning appears when no slots configured

## Change Log

| Date       | Version | Description            | Author   |
| ---------- | ------- | ---------------------- | -------- |
| 2026-01-07 | 1.0     | Initial story creation | PM Agent |
| 2026-01-09 | 1.1     | Implementation complete | James (Dev Agent) |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References

None - implementation completed without blocking issues.

### Completion Notes List

- Implemented AvailabilitySlot model in Prisma schema with relation to Experience
- Created time-slots.ts constants with 30-minute increments (08:00-20:00), days of week, and utility functions
- Built availability.ts server action with getAvailabilitySlots, updateAvailabilitySlots, and toggleSlotActive
- Created TimeSlotPicker component with start/end time selectors and isActive toggle
- Created WeeklyCalendarPreview component showing visual weekly schedule
- Created AvailabilityScheduleBuilder as main component integrating all features
- Added availability section to experience edit page with section header
- Implemented overlap validation both client-side and server-side
- Added "Copy to all days" feature with AlertDialog confirmation
- Added published experience warning when no availability configured
- Created Switch UI component for slot toggles
- Added 24 unit tests for time-slots utility functions
- All 181 tests pass, build succeeds, lint passes

### File List

| File | Action |
|------|--------|
| prisma/schema.prisma | Modified - Added AvailabilitySlot model and Experience relation |
| src/lib/constants/time-slots.ts | Created - Time slot constants and utility functions |
| src/server/actions/availability.ts | Created - Server actions for availability CRUD |
| src/components/ui/switch.tsx | Created - shadcn Switch component |
| src/components/features/experience/TimeSlotPicker.tsx | Created - Time slot picker UI |
| src/components/features/experience/WeeklyCalendarPreview.tsx | Created - Weekly calendar visualization |
| src/components/features/experience/AvailabilityScheduleBuilder.tsx | Created - Main availability builder |
| src/app/(protected)/dashboard/experiences/[id]/edit/page.tsx | Modified - Added availability section |
| tests/unit/lib/constants/time-slots.test.ts | Created - Unit tests for time-slots |

## QA Results

### Review Date: 2026-01-09

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Overall: Excellent** - Well-architected feature with proper separation of concerns, comprehensive validation, and solid test coverage for utility functions.

**Strengths:**
- Prisma schema follows story spec exactly with proper index
- Server actions follow `ActionResult<T>` pattern consistently
- Both client-side and server-side overlap validation
- Time format and day-of-week validation on server
- Transaction used for atomic slot updates
- Clean component architecture with single-responsibility components
- European week ordering (Monday-Sunday) per business requirements
- 24 unit tests for time-slots utilities with excellent edge case coverage

**Architecture:**
- `time-slots.ts` - Pure utility functions, well-tested
- `availability.ts` - Server actions with full validation
- `AvailabilityScheduleBuilder` - Coordinator component
- `TimeSlotPicker` - Slot editing with toggle
- `WeeklyCalendarPreview` - Visual display component

### Refactoring Performed

None required - code quality meets standards.

### Compliance Check

- Coding Standards: ✓ Follows ActionResult pattern, TypeScript strict mode, proper imports
- Project Structure: ✓ Files in correct locations per source-tree spec
- Testing Strategy: ✓ Unit tests for utilities, server actions follow established patterns
- All ACs Met: ✓ All 12 acceptance criteria implemented

### AC Traceability Matrix

| AC | Description | Implementation | Test Coverage |
|----|-------------|----------------|---------------|
| AC1 | Prisma AvailabilitySlot model | `schema.prisma:182-195` | - |
| AC2 | Availability section on edit page | `page.tsx:102-123` | - |
| AC3 | Weekly schedule builder UI | `AvailabilityScheduleBuilder.tsx` | - |
| AC4 | Time slot picker (30-min increments) | `TimeSlotPicker.tsx` | ✓ TIME_SLOTS tests |
| AC5 | Multiple slots per day | Full support in all components | - |
| AC6 | Toggle enable/disable slots | `TimeSlotPicker` Switch component | - |
| AC7 | Weekly calendar preview | `WeeklyCalendarPreview.tsx` | - |
| AC8 | Overlap validation | `hasOverlappingSlots` + server validation | ✓ 6 tests |
| AC9 | Copy to all days | `AvailabilityScheduleBuilder:104-126` | - |
| AC10 | Empty state | `AvailabilityScheduleBuilder:206-214` | - |
| AC11 | Published warning | `AvailabilityScheduleBuilder:192-203` | - |
| AC12 | Independent save | `updateAvailabilitySlots` action | - |

### Improvements Checklist

- [x] Prisma schema with proper relation and index
- [x] Auth checks on all server actions
- [x] Ownership validation for all operations
- [x] Server-side overlap validation
- [x] Time format validation (HH:mm regex)
- [x] Day of week bounds checking (0-6)
- [x] Start/end time order validation
- [x] Atomic updates via transaction
- [x] Loading and saving states in UI
- [x] Unsaved changes indicator
- [ ] Consider adding integration tests for availability server actions

### Security Review

**Status: PASS** - No vulnerabilities identified.

- ✓ Authentication required for all operations
- ✓ Authorization checked (winery ownership via experience relation)
- ✓ Input validation: time format, day bounds, time order
- ✓ No SQL injection (Prisma ORM)
- ✓ Cascade delete properly configured

### Performance Considerations

**Status: PASS** - No issues identified.

- ✓ Database index on `(experienceId, dayOfWeek)` for efficient queries
- ✓ Ordered query results (dayOfWeek, startTime) for consistent display
- ✓ Client-side validation prevents unnecessary server roundtrips
- ✓ Single transaction for bulk slot updates

### Files Modified During Review

None - no refactoring required.

### Gate Status

Gate: **PASS** → `docs/qa/gates/2.3-experience-availability.yml`

### Recommended Status

✓ **Ready for Done** - All acceptance criteria implemented, tests passing, validation comprehensive.
