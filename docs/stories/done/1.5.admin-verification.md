# Story 1.5: Admin Winemaker Verification

## Status

Done


## Story

**As a** platform administrator,
**I want** to review and verify pending winemaker registrations,
**so that** only legitimate wineries appear on the platform.

## Acceptance Criteria

1. Admin role check middleware for `/admin/*` routes
2. Admin dashboard page (`/admin`) with navigation to verification queue
3. Pending wineries list (`/admin/wineries/pending`) showing: name, commune, registration date, applicant email
4. Winery detail view (`/admin/wineries/[id]`) showing all submitted information
5. "Approve" action sets status to `VERIFIED` and timestamps verification date
6. "Reject" action sets status to `REJECTED` with required reason text field
7. Approved winemakers receive email notification (template: "Your winery has been verified!")
8. Rejected winemakers receive email with rejection reason
9. Filter/sort options: by date, by commune
10. Count badge showing number of pending verifications
11. Verification action logged with admin user ID and timestamp
12. Initial admin user seeded in development database

## Tasks / Subtasks

- [x] Extend Prisma schema (AC: 5, 11)
  - [x] Add `verifiedAt` and `verifiedBy` fields to Winery model
  - [x] Add `rejectionReason` field to Winery model
  - [x] Create `VerificationLog` model for audit trail
  - [x] Run migration
- [x] Create admin middleware (AC: 1)
  - [x] Add admin role check to middleware.ts for `/admin/*` routes
  - [x] Redirect non-admins to home with error message
- [x] Build admin dashboard (AC: 2)
  - [x] Create `/app/admin/page.tsx`
  - [x] Add navigation to verification queue
  - [x] Show summary stats
- [x] Build pending wineries list (AC: 3, 9, 10)
  - [x] Create `/app/admin/wineries/pending/page.tsx`
  - [x] Create PendingWineriesTable component
  - [x] Display name, commune, registration date, applicant email
  - [x] Add filter by commune dropdown
  - [x] Add sort by date option
  - [x] Show count badge in navigation
- [x] Build winery detail view (AC: 4)
  - [x] Create `/app/admin/wineries/[id]/page.tsx`
  - [x] Display all submitted information
  - [x] Show applicant details
- [x] Implement approve action (AC: 5, 7, 11)
  - [x] Create `approveWinery` server action
  - [x] Set status to `VERIFIED`
  - [x] Set `verifiedAt` timestamp and `verifiedBy` admin ID
  - [x] Log verification action
  - [x] Send approval email to winemaker
- [x] Implement reject action (AC: 6, 8, 11)
  - [x] Create `rejectWinery` server action with reason parameter
  - [x] Set status to `REJECTED`
  - [x] Store rejection reason
  - [x] Log rejection action
  - [x] Send rejection email with reason
- [x] Create email templates (AC: 7, 8)
  - [x] Create WineryApprovedEmail template
  - [x] Create WineryRejectedEmail template
- [x] Seed admin user (AC: 12)
  - [x] Add admin user to `prisma/seed.ts`
  - [x] Document admin credentials for development

## Dev Notes

### Tech Stack Reference

- **Email:** Resend with React Email templates
- **UI:** shadcn/ui tables and components
- **Database:** Prisma 5.x

### Source Tree Reference

```
src/
├── app/
│   └── admin/
│       ├── page.tsx
│       ├── layout.tsx
│       └── wineries/
│           ├── pending/
│           │   └── page.tsx
│           └── [id]/
│               └── page.tsx
├── components/
│   └── features/
│       └── admin/
│           ├── PendingWineriesTable.tsx
│           ├── WineryDetailView.tsx
│           └── AdminStats.tsx
├── server/
│   ├── actions/
│   │   └── admin.ts
│   └── emails/
│       ├── WineryApprovedEmail.tsx
│       └── WineryRejectedEmail.tsx
```

### Prisma Schema Additions

```prisma
model Winery {
  // ... existing fields
  verifiedAt      DateTime?
  verifiedBy      String?
  rejectionReason String?
}

model VerificationLog {
  id        String   @id @default(cuid())
  wineryId  String
  action    String   // 'APPROVED' | 'REJECTED'
  adminId   String
  reason    String?
  createdAt DateTime @default(now())
}
```

### Email Service Usage

```typescript
import { Resend } from 'resend';
import { WineryApprovedEmail } from '@/server/emails/WineryApprovedEmail';

const resend = new Resend(env.RESEND_API_KEY);
await resend.emails.send({
  from: 'EnCave <noreply@encave.ch>',
  to: winemaker.email,
  subject: 'Your winery has been verified!',
  react: WineryApprovedEmail({ wineryName: winery.name }),
});
```

### Testing

**Test Location:** `tests/unit/`, `tests/integration/`
**Frameworks:** Vitest

**Test Cases:**

- Non-admin users cannot access `/admin/*` routes
- Approve action updates status and sends email
- Reject action requires reason field
- Verification log created on both approve and reject
- Pending count updates after actions

## Change Log

| Date       | Version | Description            | Author   |
| ---------- | ------- | ---------------------- | -------- |
| 2026-01-07 | 1.0     | Initial story creation | PM Agent |
| 2026-01-09 | 1.1     | Post-QA fixes: XSS, Zod validation, email error handling | Dev Agent |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References

None - no debugging issues encountered.

### Completion Notes List

- Extended Prisma schema with `verifiedAt`, `verifiedBy`, `rejectionReason` fields on Winery model and created `VerificationLog` model
- Admin middleware added to protect `/admin/*` routes, redirects non-admin users to home with error parameter
- Admin dashboard shows summary stats (pending, verified, rejected, total) with navigation and recent pending wineries
- Pending wineries list includes table with filter by commune dropdown and sort by date toggle
- Winery detail view shows all submitted information, applicant details, photos, with approve/reject buttons
- Email templates implemented inline in email service (HTML templates for approved/rejected notifications)
- Admin user seeded via `prisma/seed.ts` (admin@encave.ch / admin123!)
- All 86 tests pass including 13 new admin action integration tests
- Build completes successfully

**Post-QA Fixes (2026-01-09):**
- Fixed XSS vulnerability in email templates - added `escapeHtml()` function to sanitize wineryName and reason before HTML interpolation
- Added Zod schema validation (`ApproveWinerySchema`, `RejectWinerySchema`) to server actions per coding standards
- Improved email failure handling - failures now logged with context but don't fail the action

### File List

**New Files:**
- `src/app/admin/layout.tsx` - Admin layout with navigation and pending count badge
- `src/app/admin/page.tsx` - Admin dashboard with stats and recent pending wineries
- `src/app/admin/wineries/pending/page.tsx` - Pending wineries list page
- `src/app/admin/wineries/[id]/page.tsx` - Winery detail review page
- `src/components/features/admin/AdminStats.tsx` - Stats cards component
- `src/components/features/admin/PendingWineriesTable.tsx` - Filterable/sortable table component
- `src/components/features/admin/WineryDetailView.tsx` - Detail view with approve/reject actions
- `src/server/actions/admin.ts` - approveWinery and rejectWinery server actions
- `src/server/services/email.service.ts` - Email service with Resend integration
- `prisma/seed.ts` - Database seed script with admin user
- `tests/integration/actions/admin.test.ts` - Admin action integration tests

**Modified Files:**
- `prisma/schema.prisma` - Added verification fields and VerificationLog model
- `src/middleware.ts` - Added admin route protection
- `src/lib/env.ts` - Added RESEND_API_KEY environment variable
- `package.json` - Added resend, @react-email/components, tsx dependencies and seed script

## QA Results

### Review Date: 2026-01-09

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

Overall implementation quality is **solid**. The admin verification feature is well-structured with proper separation of concerns, defense-in-depth auth checks, and atomic transactions. Server actions follow the ActionResult pattern correctly. The 13 new integration tests provide good coverage of the critical approve/reject flows. Build and all 86 tests pass.

### Acceptance Criteria Traceability

| AC | Description | Status | Validation |
|----|-------------|--------|------------|
| 1 | Admin role middleware | ✅ | `middleware.ts:21-39` + layout server check |
| 2 | Admin dashboard | ✅ | `admin/page.tsx` with stats and navigation |
| 3 | Pending list | ✅ | `pending/page.tsx` + PendingWineriesTable |
| 4 | Detail view | ✅ | `[id]/page.tsx` + WineryDetailView |
| 5 | Approve action | ✅ | `admin.ts:14-92`, transaction + timestamp |
| 6 | Reject action | ✅ | `admin.ts:97-183`, reason required + stored |
| 7 | Approved email | ✅ | `email.service.ts:42-97` template |
| 8 | Rejected email | ✅ | `email.service.ts:99-147` with reason |
| 9 | Filter/sort | ✅ | PendingWineriesTable commune filter + date sort |
| 10 | Count badge | ✅ | `layout.tsx:49-53` in navigation |
| 11 | Verification log | ✅ | VerificationLog model + transaction |
| 12 | Admin seeded | ✅ | `seed.ts` with admin@encave.ch |

### Refactoring Performed

None - no refactoring performed during this review.

### Compliance Check

- Coding Standards: ⚠️ Partial - ActionResult pattern followed, but i18n requirement not met (hardcoded strings)
- Project Structure: ✅ Follows source tree structure
- Testing Strategy: ✅ Integration tests for server actions
- All ACs Met: ✅ All 12 acceptance criteria implemented

### Improvements Checklist

- [ ] **i18n Compliance**: Replace hardcoded admin UI strings with next-intl translations (coding standards require all user-facing strings via next-intl) - *Acceptable tech debt for admin-only interface*
- [x] **Email Failure Handling**: ~~Propagate email send failures~~ - FIXED: Now logs warning when email fails (`admin.ts:97-101,198-202`)
- [x] **Input Validation**: ~~Add Zod schema validation~~ - FIXED: Added `ApproveWinerySchema` and `RejectWinerySchema` (`admin.ts:12-19`)
- [ ] **Seed Security**: Move admin credentials to environment variables instead of hardcoded in `seed.ts:13` - *Low priority, dev-only file*
- [ ] **UI Tests**: Consider adding component tests for PendingWineriesTable filter/sort behavior

### Security Review

**Findings:**
1. ✅ **Authorization**: Double auth check (middleware + server action) - defense in depth
2. ✅ **Transaction Safety**: Atomic updates prevent partial state
3. ✅ **Input Validation**: Zod schemas validate all inputs before processing
4. ⚠️ **Email Template XSS**: `wineryName` and `reason` interpolated directly into HTML without escaping (`email.service.ts:60,122`). Low risk as emails are not rendered in web context.
5. ⚠️ **Seed Credentials**: Hardcoded admin password in source (`seed.ts:13`) - acceptable for dev-only seed file

### Performance Considerations

**Positive:**
- Parallel queries using `Promise.all` in dashboard page
- Efficient selective includes (only needed user fields)
- Client-side filtering avoids server round-trips

**No blocking performance issues identified.**

### Files Modified During Review

None - no files modified.

### Gate Status

Gate: **PASS** → `docs/qa/gates/1.5-admin-verification.yml`

### Recommended Status

✓ **Ready for Done**

All medium and high severity issues have been addressed. Remaining items are low-priority technical debt acceptable for MVP:
- i18n for admin-only interface (future enhancement)
- Seed credentials (dev-only file)

(Story owner decides final status)
