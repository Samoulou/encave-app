# Story 3.5: Booking Cancellation & Refunds

## Status

Ready for Review

## Story

**As a** visitor,
**I want** to cancel my booking and receive a refund if eligible,
**so that** I'm not charged for experiences I cannot attend.

## Acceptance Criteria

1. Cancellation policy enforced: >24h before = full refund, <24h = no refund
2. "Cancel Booking" button on booking detail page (client view)
3. Cancellation modal shows:
   - Policy explanation with countdown to 24h deadline
   - Refund amount (full or zero based on policy)
   - Confirmation checkbox ("I understand this action cannot be undone")
4. On cancellation >24h: Stripe refund issued automatically, booking status → `CANCELLED_BY_CLIENT`
5. On cancellation <24h: No refund, booking status → `CANCELLED_BY_CLIENT`, winemaker still paid
6. Client cancellation confirmation email with refund details (if applicable)
7. Winemaker notification email about cancellation
8. Cancelled bookings freed up capacity for new bookings
9. Booking history shows cancelled bookings with status badge
10. Refund processing via Stripe Refund API (application fee also refunded proportionally)
11. Edge case: cancellation exactly at 24h boundary uses server timestamp
12. Admin can manually process refunds for exceptions (via Stripe dashboard for MVP)

## Tasks / Subtasks

- [x] Build cancellation button (AC: 2)
  - [x] Add "Cancel Booking" button to booking detail page
  - [x] Only show for CONFIRMED bookings
  - [x] Only show before experience date (requires access token)
- [x] Build cancellation modal (AC: 3)
  - [x] Create CancellationModal component
  - [x] Show policy explanation
  - [x] Calculate and show countdown to 24h deadline
  - [x] Show refund amount based on policy
  - [x] Add confirmation checkbox
- [x] Implement cancellation logic (AC: 1, 4, 5, 11)
  - [x] Create `cancelBooking` server action
  - [x] Calculate if >24h before experience (server timestamp)
  - [x] Determine refund eligibility
  - [x] Update booking status to CANCELLED_BY_CLIENT
- [x] Implement Stripe refund (AC: 4, 10)
  - [x] Create refund via Stripe Refund API
  - [x] Refund application fee proportionally
  - [x] Handle refund errors
- [x] Build client cancellation email (AC: 6)
  - [x] Create BookingCancellationEmail template (already exists)
  - [x] Include refund details if applicable
  - [x] Include cancellation confirmation
- [x] Build winemaker notification (AC: 7)
  - [x] Create WinemakerCancellationEmail template (already exists)
  - [x] Notify about cancellation
  - [x] Include booking details
- [x] Update capacity (AC: 8)
  - [x] Ensure cancelled bookings don't count against capacity (already implemented)
  - [x] Update availability checks
- [x] Update booking history (AC: 9)
  - [x] Show cancelled status badge (booking detail page already shows status)
  - [x] Display cancellation details

## Dev Notes

### Tech Stack Reference

- **Refunds:** Stripe Refund API
- **Email:** Resend with React Email
- **Time:** date-fns for time calculations

### Source Tree Reference

```
src/
├── components/
│   └── features/
│       └── booking/
│           ├── CancelBookingButton.tsx
│           └── CancellationModal.tsx
├── server/
│   ├── actions/
│   │   └── booking.ts
│   ├── services/
│   │   └── booking.service.ts
│   └── emails/
│       ├── BookingCancellationEmail.tsx
│       └── BookingCancelledNotificationEmail.tsx
```

### Cancellation Logic

```typescript
export async function cancelBooking(
  bookingId: string
): Promise<ActionResult<Booking>> {
  const booking = await db.booking.findUnique({
    where: { id: bookingId },
    include: { experience: true },
  });

  const experienceDateTime = new Date(
    `${booking.date.toISOString().split('T')[0]}T${booking.timeSlot}`
  );
  const hoursUntilExperience = differenceInHours(
    experienceDateTime,
    new Date()
  );
  const isEligibleForRefund = hoursUntilExperience > 24;

  if (isEligibleForRefund) {
    // Process full refund
    await stripe.refunds.create({
      payment_intent: booking.stripePaymentIntentId,
      refund_application_fee: true,
    });
  }

  await db.booking.update({
    where: { id: bookingId },
    data: {
      status: 'CANCELLED_BY_CLIENT',
      cancelledAt: new Date(),
      refundIssued: isEligibleForRefund,
    },
  });

  // Send emails...

  return { success: true, data: booking };
}
```

### Capacity Query Update

```typescript
// Only count non-cancelled bookings
const bookedGuests = await db.booking.aggregate({
  where: {
    experienceId,
    date,
    timeSlot,
    status: { in: ['PENDING_PAYMENT', 'CONFIRMED'] }, // Excludes CANCELLED
  },
  _sum: { guestCount: true },
});
```

### Testing

**Test Location:** `tests/unit/`, `tests/integration/`
**Frameworks:** Vitest

**Test Cases:**

- Cancellation >24h issues full refund
- Cancellation <24h issues no refund
- Cancellation at exactly 24h uses server timestamp
- Booking status updates correctly
- Capacity freed after cancellation
- Client receives cancellation email
- Winemaker receives notification

## Change Log

| Date       | Version | Description            | Author   |
| ---------- | ------- | ---------------------- | -------- |
| 2026-01-07 | 1.0     | Initial story creation | PM Agent |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References

None

### Completion Notes List

- Implemented full cancellation flow with Stripe refund integration
- Refund policy: >24h = full refund (including application fee), <24h = no refund
- Added cancelBooking and getCancellationInfo server actions in booking.ts
- Added processRefund function to payment.service.ts
- Created Dialog component (was missing from UI library)
- Added PAYMENT_FAILED error code to types/actions.ts
- Added cancellation schema fields to Prisma (cancelledAt, refundIssued, refundAmount, stripeRefundId)
- Updated all three locale files (en, fr, de) with cancellation translations
- Email templates (BookingCancellationEmail, WinemakerCancellationEmail) were already implemented in Story 3.4
- Capacity queries already exclude cancelled bookings (only count PENDING_PAYMENT and CONFIRMED)
- Cancel button only appears when user has access token (from email link)

### File List

**New Files:**
- src/components/features/booking/CancelBookingButton.tsx
- src/components/features/booking/CancellationModal.tsx
- src/components/ui/dialog.tsx

**Modified Files:**
- prisma/schema.prisma (added cancellation fields to Booking model)
- src/server/actions/booking.ts (added cancelBooking, getCancellationInfo actions)
- src/server/services/payment.service.ts (added processRefund function)
- src/components/features/booking/index.ts (exported new components)
- src/app/[locale]/(public)/booking/[id]/page.tsx (added CancelBookingButton)
- src/types/actions.ts (added PAYMENT_FAILED error code)
- messages/en.json (added cancellation translations)
- messages/fr.json (added cancellation translations)
- messages/de.json (added cancellation translations)

## QA Results

### Review Date: 2026-01-11

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

The implementation is **well-structured** with comprehensive cancellation and refund functionality. The server actions follow the established ActionResult pattern, and the UI components are properly built with React hooks and proper state management.

**Strengths:**
- Token-based access using SHA-256 hash comparison (consistent with Story 3.4)
- 24h refund policy correctly implemented using `differenceInHours`
- Refund processing includes application fee refund (`refund_application_fee: true`)
- Edge cases handled: past bookings, non-CONFIRMED status, experience already passed
- Proper error handling with specific error codes (NOT_FOUND, VALIDATION_ERROR, PAYMENT_FAILED)
- Email notifications sent to both client and winemaker
- Cancellation modal includes confirmation checkbox before action
- Capacity correctly freed (only CONFIRMED/PENDING_PAYMENT counted)

### Refactoring Performed

None - code quality is acceptable.

### Compliance Check

- Coding Standards: ✓ Follows ActionResult pattern, proper TypeScript
- Project Structure: ✓ Components, actions, services properly organized
- Testing Strategy: ✓ 26 tests cover cancellation flow and refund processing
- All ACs Met: ✓ Core ACs implemented and tested

### Requirements Traceability

| AC# | Description | Implementation | Test Coverage |
|-----|-------------|----------------|---------------|
| 1 | Cancellation policy enforced | `booking.ts:505-506` | ✓ 3 tests (>24h, <24h, exactly 24h) |
| 2 | Cancel button on detail page | `CancelBookingButton.tsx` | N/A (UI) |
| 3 | Cancellation modal | `CancellationModal.tsx` | N/A (UI) |
| 4 | Stripe refund >24h | `booking.ts:511-526` | ✓ 3 tests |
| 5 | No refund <24h | `booking.ts:506` (isEligibleForRefund) | ✓ 2 tests |
| 6 | Client cancellation email | `booking.ts:545-555` | ✓ 1 test |
| 7 | Winemaker notification | `booking.ts:558-569` | ✓ 1 test |
| 8 | Capacity freed | Existing query excludes CANCELLED | ✓ Covered by capacity tests |
| 9 | Status badge | Booking detail page shows status | N/A (UI) |
| 10 | Refund API with app fee | `payment.service.ts:191-194` | ✓ 7 tests |
| 11 | Server timestamp for boundary | `booking.ts:492` (new Date()) | ✓ 1 test |
| 12 | Manual refunds via Stripe | N/A (Stripe dashboard for MVP) | N/A |

### Security Review

**PASS:**
- ✓ Token verified using SHA-256 hash before cancellation allowed
- ✓ Both plain token and hash checked for flexibility
- ✓ Only CONFIRMED bookings can be cancelled
- ✓ Past bookings rejected
- ✓ Refund errors properly handled without exposing internal details

### Improvements Checklist

- [x] Token-based access with SHA-256 verification
- [x] 24h refund boundary using server timestamp
- [x] Stripe refund with application fee refund
- [x] Status update to CANCELLED_BY_CLIENT
- [x] Cancellation fields in Prisma (cancelledAt, refundIssued, refundAmount, stripeRefundId)
- [x] Client cancellation email
- [x] Winemaker notification email
- [x] Confirmation checkbox before cancellation
- [x] Edge cases: past booking, non-confirmed status
- [x] Integration tests for cancelBooking action (12 tests)
- [x] Integration tests for getCancellationInfo action (7 tests)
- [x] Unit tests for processRefund function (7 tests)

### Test Files Added

- `tests/integration/actions/booking-cancellation.test.ts` (19 tests)
- `tests/unit/server/services/payment.service.test.ts` (+7 processRefund tests)

### Files Modified During Review

- `tests/unit/server/services/payment.service.test.ts` - Added processRefund tests

### Gate Status

Gate: **PASS** → `docs/qa/gates/3.5-booking-cancellation.yml`

### Recommended Status

**✓ Ready for Done**

All core cancellation and refund functionality is implemented with proper security controls and comprehensive test coverage. The 26 tests cover the cancellation flow, 24h boundary edge cases, refund processing, and error handling.

## Story Definition of Done Checklist

### 1. Requirements Met
- [x] All functional requirements specified in the story are implemented
- [x] All acceptance criteria defined in the story are met
  - AC1: Cancellation policy enforced (>24h = full refund, <24h = no refund)
  - AC2: Cancel button on booking detail page
  - AC3: Cancellation modal with policy, countdown, refund amount, checkbox
  - AC4: Stripe refund for >24h cancellations
  - AC5: No refund for <24h, status updated
  - AC6: Client cancellation email
  - AC7: Winemaker notification email
  - AC8: Cancelled bookings free capacity
  - AC9: Status badge for cancelled bookings
  - AC10: Stripe Refund API with application fee refund
  - AC11: Server timestamp for boundary
  - AC12: Manual refunds via Stripe dashboard (MVP)

### 2. Coding Standards & Project Structure
- [x] Code adheres to Operational Guidelines
- [x] Code aligns with Project Structure
- [x] Tech Stack compliance (Stripe, React Email, date-fns)
- [x] API and Data Models followed
- [x] Security best practices applied (token-based access, input validation)
- [x] No new linter errors (only existing console warnings in other files)
- [x] Code well-commented where necessary

### 3. Testing
- [x] All unit tests pass (385 tests)
- [x] Integration tests pass
- [x] TypeScript type checking passes
- [N/A] E2E tests (not required for this story)

### 4. Functionality & Verification
- [x] TypeScript compilation verified
- [x] Edge cases handled (past bookings, non-confirmed status, 24h boundary)

### 5. Story Administration
- [x] All tasks marked complete
- [x] Decisions documented in completion notes
- [x] Story wrap-up completed

### 6. Dependencies, Build & Configuration
- [x] Project builds without errors (TypeScript passes)
- [x] Linting passes
- [x] No new dependencies added (@radix-ui/react-dialog already installed)
- [x] New Prisma fields added (cancelledAt, refundIssued, refundAmount, stripeRefundId)
- [x] No security vulnerabilities introduced

### 7. Documentation
- [x] Inline documentation in new code
- [N/A] User-facing documentation (no changes needed)
- [N/A] Technical documentation (follows existing patterns)

### Final Confirmation
- [x] Developer Agent confirms all applicable items addressed
