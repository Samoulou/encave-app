# Story 1.2: Authentication System

## Status

Ready

## Story

**As a** user,
**I want** to create an account and log in securely,
**so that** I can access personalized features of the platform.

## Acceptance Criteria

1. NextAuth.js (Auth.js) configured with Credentials provider for email/password
2. Prisma schema extended with password hash field and `Account`, `Session` models for NextAuth
3. Password hashing implemented using bcrypt with minimum 10 rounds
4. Login page (`/login`) with email and password fields, validation errors displayed inline
5. Registration page (`/register`) with email, password, confirm password, and name fields
6. Password requirements enforced: minimum 8 characters, at least one number
7. JWT session strategy configured with 7-day expiration
8. Protected route middleware redirects unauthenticated users to `/login`
9. Session accessible in Server Components via `auth()` helper
10. Logout functionality clears session and redirects to home page
11. User role enum created: `CLIENT`, `WINEMAKER`, `ADMIN` (default: `CLIENT`)
12. Auth error messages are user-friendly (no technical jargon)

## Tasks / Subtasks

- [ ] Configure NextAuth.js v5 (AC: 1, 7)
  - [ ] Install `next-auth@beta` and `@auth/prisma-adapter`
  - [ ] Create `src/server/auth.ts` with NextAuth configuration
  - [ ] Configure Credentials provider for email/password
  - [ ] Set JWT session strategy with 7-day expiration
- [ ] Extend Prisma schema (AC: 2, 11)
  - [ ] Add `passwordHash` field to User model
  - [ ] Add `Account`, `Session`, `VerificationToken` models
  - [ ] Create UserRole enum: `CLIENT`, `WINEMAKER`, `ADMIN`
  - [ ] Run migration
- [ ] Implement password security (AC: 3, 6)
  - [ ] Install bcrypt library
  - [ ] Create password hashing utility (min 10 rounds)
  - [ ] Create Zod schema for password validation (8+ chars, 1+ number)
- [ ] Build login page (AC: 4, 12)
  - [ ] Create `/app/(auth)/login/page.tsx`
  - [ ] Build LoginForm component with email/password fields
  - [ ] Implement inline validation error display
  - [ ] Add user-friendly error messages
- [ ] Build registration page (AC: 5, 12)
  - [ ] Create `/app/(auth)/register/page.tsx`
  - [ ] Build RegisterForm with name, email, password, confirm password
  - [ ] Implement form validation with Zod
  - [ ] Add user-friendly error messages
- [ ] Create auth middleware (AC: 8)
  - [ ] Create middleware.ts for protected route handling
  - [ ] Configure redirect to `/login` for unauthenticated access
- [ ] Implement session access (AC: 9)
  - [ ] Export `auth()` helper from auth.ts
  - [ ] Document usage in Server Components
- [ ] Add logout functionality (AC: 10)
  - [ ] Create signOut action
  - [ ] Add logout button to header (when logged in)
  - [ ] Redirect to home on logout

## Dev Notes

### Tech Stack Reference

- **Auth:** NextAuth.js v5 (Auth.js)
- **Session:** JWT strategy (not database sessions)
- **Password:** bcrypt with 10+ rounds

### Source Tree Reference

```
src/
├── app/
│   └── (auth)/
│       ├── login/
│       │   └── page.tsx
│       ├── register/
│       │   └── page.tsx
│       └── layout.tsx
├── components/
│   └── features/
│       └── auth/
│           ├── LoginForm.tsx
│           ├── RegisterForm.tsx
│           └── UserMenu.tsx
├── server/
│   ├── auth.ts          # NextAuth config
│   └── actions/
│       └── auth.ts      # Auth server actions
└── lib/
    └── validators/
        └── auth.ts      # Zod schemas
```

### Server Action Pattern

All auth actions must return `ActionResult<T>`:

```typescript
type ActionResult<T> =
  | { success: true; data: T }
  | { success: false; error: { code: ErrorCode; message: string } };
```

### Coding Standards

- Use `'use server'` directive for auth actions
- All user-facing strings via next-intl
- Form validation with Zod + React Hook Form

### Testing

**Test Location:** `tests/unit/`, `tests/integration/`
**Frameworks:** Vitest, React Testing Library

**Test Cases:**

- Password validation schema rejects weak passwords
- Login form displays validation errors
- Registration creates user with hashed password
- Protected routes redirect unauthenticated users

## Change Log

| Date       | Version | Description            | Author   |
| ---------- | ------- | ---------------------- | -------- |
| 2026-01-07 | 1.0     | Initial story creation | PM Agent |

## Dev Agent Record

### Agent Model Used

_To be filled during implementation_

### Debug Log References

_To be filled during implementation_

### Completion Notes List

_To be filled during implementation_

### File List

_To be filled during implementation_

## QA Results

_To be filled after QA review_
