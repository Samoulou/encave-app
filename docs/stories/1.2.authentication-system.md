# Story 1.2: Authentication System

## Status

Ready for Review

## Story

**As a** user,
**I want** to create an account and log in securely,
**so that** I can access personalized features of the platform.

## Acceptance Criteria

1. NextAuth.js (Auth.js) configured with Credentials provider for email/password
2. Prisma schema extended with password hash field and `Account`, `Session` models for NextAuth
3. Password hashing implemented using bcrypt with minimum 10 rounds
4. Login page (`/login`) with email and password fields, validation errors displayed inline
5. Registration page (`/register`) with email, password, confirm password, and name fields
6. Password requirements enforced: minimum 8 characters, at least one number
7. JWT session strategy configured with 7-day expiration
8. Protected route middleware redirects unauthenticated users to `/login`
9. Session accessible in Server Components via `auth()` helper
10. Logout functionality clears session and redirects to home page
11. User role enum created: `CLIENT`, `WINEMAKER`, `ADMIN` (default: `CLIENT`)
12. Auth error messages are user-friendly (no technical jargon)

## Tasks / Subtasks

- [x] Configure NextAuth.js v5 (AC: 1, 7)
  - [x] Install `next-auth@beta` and `@auth/prisma-adapter`
  - [x] Create `src/server/auth.ts` with NextAuth configuration
  - [x] Configure Credentials provider for email/password
  - [x] Set JWT session strategy with 7-day expiration
- [x] Extend Prisma schema (AC: 2, 11)
  - [x] Add `passwordHash` field to User model
  - [x] Add `Account`, `Session`, `VerificationToken` models
  - [x] Create UserRole enum: `CLIENT`, `WINEMAKER`, `ADMIN`
  - [x] Run migration
- [x] Implement password security (AC: 3, 6)
  - [x] Install bcrypt library
  - [x] Create password hashing utility (min 10 rounds)
  - [x] Create Zod schema for password validation (8+ chars, 1+ number)
- [x] Build login page (AC: 4, 12)
  - [x] Create `/app/(auth)/login/page.tsx`
  - [x] Build LoginForm component with email/password fields
  - [x] Implement inline validation error display
  - [x] Add user-friendly error messages
- [x] Build registration page (AC: 5, 12)
  - [x] Create `/app/(auth)/register/page.tsx`
  - [x] Build RegisterForm with name, email, password, confirm password
  - [x] Implement form validation with Zod
  - [x] Add user-friendly error messages
- [x] Create auth middleware (AC: 8)
  - [x] Create middleware.ts for protected route handling
  - [x] Configure redirect to `/login` for unauthenticated access
- [x] Implement session access (AC: 9)
  - [x] Export `auth()` helper from auth.ts
  - [x] Document usage in Server Components
- [x] Add logout functionality (AC: 10)
  - [x] Create signOut action
  - [x] Add logout button to header (when logged in)
  - [x] Redirect to home on logout

## Dev Notes

### Tech Stack Reference

- **Auth:** NextAuth.js v5 (Auth.js)
- **Session:** JWT strategy (not database sessions)
- **Password:** bcrypt with 10+ rounds

### Source Tree Reference

```
src/
├── app/
│   └── (auth)/
│       ├── login/
│       │   └── page.tsx
│       ├── register/
│       │   └── page.tsx
│       └── layout.tsx
├── components/
│   └── features/
│       └── auth/
│           ├── LoginForm.tsx
│           ├── RegisterForm.tsx
│           └── UserMenu.tsx
├── server/
│   ├── auth.ts          # NextAuth config
│   └── actions/
│       └── auth.ts      # Auth server actions
└── lib/
    └── validators/
        └── auth.ts      # Zod schemas
```

### Server Action Pattern

All auth actions must return `ActionResult<T>`:

```typescript
type ActionResult<T> =
  | { success: true; data: T }
  | { success: false; error: { code: ErrorCode; message: string } };
```

### Coding Standards

- Use `'use server'` directive for auth actions
- All user-facing strings via next-intl
- Form validation with Zod + React Hook Form

### Testing

**Test Location:** `tests/unit/`, `tests/integration/`
**Frameworks:** Vitest, React Testing Library

**Test Cases:**

- Password validation schema rejects weak passwords
- Login form displays validation errors
- Registration creates user with hashed password
- Protected routes redirect unauthenticated users

## Change Log

| Date       | Version | Description            | Author   |
| ---------- | ------- | ---------------------- | -------- |
| 2026-01-07 | 1.0     | Initial story creation | PM Agent |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References

None - resolved type conflicts during implementation

### Completion Notes List

- NextAuth.js v5 (beta) configured with Credentials provider
- JWT session strategy with 7-day expiration
- Bcrypt password hashing with 10 rounds
- User role enum: CLIENT (default), WINEMAKER, ADMIN
- Login/Register forms with Zod validation and React Hook Form
- Middleware protects /dashboard routes
- Header shows auth state with login/logout
- Tests passing: 18/18 (including 11 new auth validator tests)
- Build: Successful
- Lint: No errors

### File List

**Created:**
- `src/server/auth.ts` - NextAuth configuration
- `src/server/password.ts` - Bcrypt hashing utilities
- `src/server/actions/auth.ts` - Login/register/logout actions
- `src/lib/validators/auth.ts` - Zod schemas for auth
- `src/types/actions.ts` - ActionResult type
- `src/middleware.ts` - Protected route middleware
- `src/app/(auth)/layout.tsx` - Auth pages layout
- `src/app/(auth)/login/page.tsx` - Login page
- `src/app/(auth)/register/page.tsx` - Register page
- `src/app/api/auth/[...nextauth]/route.ts` - NextAuth API route
- `src/components/features/auth/LoginForm.tsx` - Login form component
- `src/components/features/auth/RegisterForm.tsx` - Register form component
- `src/components/features/auth/UserMenu.tsx` - User menu with logout
- `src/components/layout/Header.tsx` - Site header with auth state
- `tests/unit/lib/auth-validators.test.ts` - Auth validation tests

**Modified:**
- `prisma/schema.prisma` - Added auth models (Account, Session, VerificationToken)
- `src/app/page.tsx` - Added Header component
- `src/types/index.ts` - Export UserRole and ActionResult
- `.env.local` - Added AUTH_SECRET, AUTH_URL
- `.env.example` - Updated auth environment variables

## QA Results

_To be filled after QA review_
