# Story 1.4: Winemaker Profile Management

## Status

Ready

## Story

**As a** verified winemaker,
**I want** to manage my winery's profile and photos,
**so that** visitors see accurate and attractive information.

## Acceptance Criteria

1. Winery profile page (`/dashboard/winery/profile`) accessible only to verified winemakers
2. Edit form pre-populated with current winery data
3. Photo upload component supporting JPEG/PNG, max 5MB per image
4. Cover photo field (single image, 16:9 aspect ratio recommended)
5. Gallery photos field (up to 6 images)
6. Images uploaded to Vercel Blob (or Cloudinary) with automatic optimization
7. Image preview shown before and after upload
8. Description field supports basic formatting guidance (plain text for MVP)
9. Changes saved with success toast notification
10. Validation prevents saving incomplete required fields
11. "View public profile" link to preview how visitors will see the winery
12. Last updated timestamp displayed on profile

## Tasks / Subtasks

- [ ] Extend Prisma schema for photos (AC: 4, 5)
  - [ ] Add `coverPhoto` field to Winery model
  - [ ] Create `WineryGalleryImage` model for gallery photos
  - [ ] Run migration
- [ ] Create profile page (AC: 1)
  - [ ] Create `/app/(protected)/dashboard/winery/profile/page.tsx`
  - [ ] Add verified winemaker guard (redirect if not verified)
- [ ] Build profile edit form (AC: 2, 8, 10)
  - [ ] Create WineryProfileForm component
  - [ ] Pre-populate form with current winery data
  - [ ] Add description field with formatting guidance
  - [ ] Implement Zod validation for required fields
- [ ] Implement image upload (AC: 3, 6, 7)
  - [ ] Install `@vercel/blob` package
  - [ ] Create ImageUpload component
  - [ ] Support JPEG/PNG, max 5MB validation
  - [ ] Show image preview before upload
  - [ ] Upload to Vercel Blob with optimization
- [ ] Build cover photo upload (AC: 4)
  - [ ] Add cover photo field to form
  - [ ] Show 16:9 aspect ratio guidance
  - [ ] Display current cover photo with replace option
- [ ] Build gallery upload (AC: 5)
  - [ ] Add gallery section allowing up to 6 images
  - [ ] Show grid of uploaded images
  - [ ] Allow removal and reordering
- [ ] Implement save functionality (AC: 9, 12)
  - [ ] Create `updateWineryProfile` server action
  - [ ] Show success toast on save
  - [ ] Update `updatedAt` timestamp
  - [ ] Display last updated time on page
- [ ] Add public preview link (AC: 11)
  - [ ] Add "View public profile" button
  - [ ] Link to `/wineries/[slug]` (opens in new tab)

## Dev Notes

### Tech Stack Reference

- **Storage:** Vercel Blob for image storage
- **Forms:** React Hook Form 7.x with Zod validation
- **UI:** shadcn/ui components + toast notifications

### Source Tree Reference

```
src/
├── app/
│   └── (protected)/
│       └── dashboard/
│           └── winery/
│               └── profile/
│                   └── page.tsx
├── components/
│   ├── features/
│   │   └── winery/
│   │       └── WineryProfileForm.tsx
│   └── shared/
│       └── ImageUpload.tsx
├── server/
│   ├── actions/
│   │   └── winery.ts
│   └── services/
│       └── upload.service.ts
└── lib/
    └── validators/
        └── winery.ts
```

### Vercel Blob Usage

```typescript
import { put } from '@vercel/blob';

const blob = await put(filename, file, {
  access: 'public',
  contentType: file.type,
});
// blob.url contains the public URL
```

### Image Validation Schema

```typescript
const imageSchema = z.object({
  file: z
    .instanceof(File)
    .refine((f) => f.size <= 5 * 1024 * 1024, 'Max 5MB')
    .refine(
      (f) => ['image/jpeg', 'image/png'].includes(f.type),
      'JPEG/PNG only'
    ),
});
```

### Testing

**Test Location:** `tests/unit/`, `tests/integration/`
**Frameworks:** Vitest, React Testing Library

**Test Cases:**

- Image validation rejects files > 5MB
- Image validation rejects non-JPEG/PNG files
- Profile form pre-populates with existing data
- Save action updates winery and returns success
- Unverified winemakers redirected from profile page

## Change Log

| Date       | Version | Description            | Author   |
| ---------- | ------- | ---------------------- | -------- |
| 2026-01-07 | 1.0     | Initial story creation | PM Agent |

## Dev Agent Record

### Agent Model Used

_To be filled during implementation_

### Debug Log References

_To be filled during implementation_

### Completion Notes List

_To be filled during implementation_

### File List

_To be filled during implementation_

## QA Results

_To be filled after QA review_
