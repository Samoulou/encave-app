# Story 1.3: Winemaker Registration Flow

## Status

Ready

## Story

**As a** winemaker,
**I want** to register my winery on the platform,
**so that** I can eventually offer wine experiences to visitors.

## Acceptance Criteria

1. Prisma schema extended with `Winery` model: id, name, slug, description, address, commune, phone, email, userId, status, createdAt
2. Winery status enum: `PENDING`, `VERIFIED`, `REJECTED`, `SUSPENDED`
3. Registration flow extended: after basic signup, winemakers select "I am a winemaker" option
4. Winemaker onboarding form (`/onboarding/winery`) collects: winery name, description (textarea), address, commune (dropdown of Valais communes), contact phone
5. Form validation with Zod schema (name required, description min 50 chars, valid Swiss phone format)
6. On submission, `Winery` record created with status `PENDING`, user role updated to `WINEMAKER`
7. Confirmation page explains verification process and expected timeline
8. User cannot access winemaker features until status is `VERIFIED`
9. Duplicate winery name check with user-friendly error message
10. Winery slug auto-generated from name (URL-safe, unique)

## Tasks / Subtasks

- [ ] Extend Prisma schema (AC: 1, 2)
  - [ ] Create `Winery` model with all required fields
  - [ ] Create WineryStatus enum: `PENDING`, `VERIFIED`, `REJECTED`, `SUSPENDED`
  - [ ] Add relation from User to Winery (one-to-one)
  - [ ] Run migration
- [ ] Create Valais communes data (AC: 4)
  - [ ] Create `src/lib/constants/communes.ts` with Valais commune list
  - [ ] Export as typed array for dropdown
- [ ] Build winemaker selection (AC: 3)
  - [ ] Add "I am a winemaker" option to registration flow
  - [ ] Redirect selected users to `/onboarding/winery`
- [ ] Build onboarding form (AC: 4, 5)
  - [ ] Create `/app/(protected)/onboarding/winery/page.tsx`
  - [ ] Build WineryOnboardingForm component
  - [ ] Add fields: name, description (textarea), address, commune (dropdown), phone
  - [ ] Implement Zod validation (name required, description 50+ chars, Swiss phone)
- [ ] Implement form submission (AC: 6, 9, 10)
  - [ ] Create `createWinery` server action
  - [ ] Check for duplicate winery names
  - [ ] Generate URL-safe slug from name (ensure uniqueness)
  - [ ] Create Winery with status `PENDING`
  - [ ] Update user role to `WINEMAKER`
- [ ] Build confirmation page (AC: 7)
  - [ ] Create confirmation page explaining verification process
  - [ ] Show expected timeline for review
- [ ] Implement access control (AC: 8)
  - [ ] Create middleware/guard for winemaker features
  - [ ] Check winery status is `VERIFIED` before allowing access
  - [ ] Show "pending verification" message for unverified winemakers

## Dev Notes

### Tech Stack Reference

- **Validation:** Zod 3.x for form schemas
- **Forms:** React Hook Form 7.x
- **Database:** Prisma 5.x

### Source Tree Reference

```
src/
├── app/
│   └── (protected)/
│       └── onboarding/
│           └── winery/
│               └── page.tsx
├── components/
│   └── features/
│       └── winery/
│           └── WineryOnboardingForm.tsx
├── server/
│   └── actions/
│       └── winery.ts
├── lib/
│   ├── constants/
│   │   └── communes.ts
│   └── validators/
│       └── winery.ts
└── types/
    └── winery.ts
```

### Prisma Schema Addition

```prisma
model Winery {
  id          String       @id @default(cuid())
  name        String       @unique
  slug        String       @unique
  description String
  address     String
  commune     String
  phone       String
  email       String
  userId      String       @unique
  user        User         @relation(fields: [userId], references: [id])
  status      WineryStatus @default(PENDING)
  createdAt   DateTime     @default(now())
  updatedAt   DateTime     @updatedAt
}

enum WineryStatus {
  PENDING
  VERIFIED
  REJECTED
  SUSPENDED
}
```

### Swiss Phone Validation

Valid formats: `+41 XX XXX XX XX`, `0XX XXX XX XX`

### Testing

**Test Location:** `tests/unit/`, `tests/integration/`
**Frameworks:** Vitest, React Testing Library

**Test Cases:**

- Zod schema validates Swiss phone format correctly
- Duplicate winery name returns friendly error
- Slug generation creates URL-safe unique slugs
- User role updates to WINEMAKER on submission
- Unverified winemakers cannot access dashboard features

## Change Log

| Date       | Version | Description            | Author   |
| ---------- | ------- | ---------------------- | -------- |
| 2026-01-07 | 1.0     | Initial story creation | PM Agent |

## Dev Agent Record

### Agent Model Used

_To be filled during implementation_

### Debug Log References

_To be filled during implementation_

### Completion Notes List

_To be filled during implementation_

### File List

_To be filled during implementation_

## QA Results

_To be filled after QA review_
