# Story 1.6: Public Winery Directory

## Status

Ready for Review

## Story

**As a** visitor,
**I want** to browse a list of verified wineries in Valais,
**so that** I can discover winemakers in the region.

## Acceptance Criteria

1. Public wineries page (`/wineries`) accessible without authentication
2. Grid layout displaying winery cards (photo, name, commune, short description)
3. Only `VERIFIED` wineries displayed
4. Cards link to individual winery page (`/wineries/[slug]`)
5. Individual winery page shows: cover photo, name, full description, location, contact info
6. Empty state message if no verified wineries yet: "Winemakers coming soon..."
7. Page metadata (title, description) configured for SEO
8. Server-side rendering for optimal SEO and performance
9. Basic commune filter (dropdown) to narrow results
10. Responsive layout: 1 column mobile, 2 columns tablet, 3 columns desktop
11. "Coming soon: Book experiences" teaser on winery detail page
12. Page loads in under 2 seconds (LCP metric)

## Tasks / Subtasks

- [x] Build wineries listing page (AC: 1, 2, 3, 6, 7, 8)
  - [x] Create `/app/(public)/wineries/page.tsx`
  - [x] Query only VERIFIED wineries
  - [x] Implement server-side rendering
  - [x] Add page metadata for SEO
  - [x] Create empty state component
- [x] Build winery card component (AC: 2, 4)
  - [x] Create WineryCard component
  - [x] Display photo, name, commune, truncated description
  - [x] Link to `/wineries/[slug]`
- [x] Implement responsive grid (AC: 10)
  - [x] 1 column on mobile (<640px)
  - [x] 2 columns on tablet (640-1024px)
  - [x] 3 columns on desktop (>1024px)
- [x] Build commune filter (AC: 9)
  - [x] Add commune dropdown filter
  - [x] Filter results on selection
  - [x] Use nuqs for URL state (shareable filter)
- [x] Build winery detail page (AC: 4, 5, 7, 8, 11)
  - [x] Create `/app/(public)/wineries/[slug]/page.tsx`
  - [x] Display cover photo, name, full description
  - [x] Show location with address
  - [x] Display contact information
  - [x] Add "Coming soon: Book experiences" teaser
  - [x] Configure SEO metadata with winery info
- [x] Optimize performance (AC: 12)
  - [x] Use Next.js Image component for optimization
  - [x] Implement proper caching headers
  - [x] Verify LCP < 2 seconds

## Dev Notes

### Tech Stack Reference

- **Routing:** Next.js App Router with dynamic routes
- **State:** nuqs for URL search params
- **Images:** Next.js Image with Vercel Blob URLs

### Source Tree Reference

```
src/
├── app/
│   └── (public)/
│       └── wineries/
│           ├── page.tsx
│           └── [slug]/
│               └── page.tsx
├── components/
│   ├── features/
│   │   └── winery/
│   │       ├── WineryCard.tsx
│   │       └── WineryProfile.tsx
│   └── shared/
│       └── EmptyState.tsx
└── server/
    └── queries/
        └── winery.queries.ts
```

### Database Query Pattern

```typescript
// src/server/queries/winery.queries.ts
export async function getVerifiedWineries(commune?: string) {
  return db.winery.findMany({
    where: {
      status: 'VERIFIED',
      ...(commune && { commune }),
    },
    orderBy: { name: 'asc' },
  });
}

export async function getWineryBySlug(slug: string) {
  return db.winery.findUnique({
    where: { slug, status: 'VERIFIED' },
  });
}
```

### SEO Metadata Pattern

```typescript
// In page.tsx
export async function generateMetadata({ params }): Promise<Metadata> {
  const winery = await getWineryBySlug(params.slug);
  return {
    title: `${winery.name} | EnCave`,
    description: winery.description.slice(0, 160),
    openGraph: {
      images: [winery.coverPhoto],
    },
  };
}
```

### Responsive Grid CSS

```css
/* Tailwind classes */
grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6
```

### Testing

**Test Location:** `tests/unit/`, `tests/e2e/`
**Frameworks:** Vitest, Playwright

**Test Cases:**

- Only verified wineries appear in listing
- Commune filter narrows results correctly
- Winery detail page displays all information
- Empty state shows when no wineries exist
- Page loads within performance budget

## Change Log

| Date       | Version | Description            | Author    |
| ---------- | ------- | ---------------------- | --------- |
| 2026-01-07 | 1.0     | Initial story creation | PM Agent  |
| 2026-01-09 | 1.1     | Implementation complete | Dev Agent |
| 2026-01-09 | 1.2     | QA improvements applied | Dev Agent |

## Dev Agent Record

### Agent Model Used

Claude Opus 4.5 (claude-opus-4-5-20251101)

### Debug Log References

None - implementation proceeded without blocking issues.

### Completion Notes List

- Created winery queries module for data fetching
- Built responsive WineryCard component with image optimization
- Implemented CommuneFilter using nuqs for URL state management
- Added NuqsAdapter to root layout for URL state support
- Created EmptyState shared component
- Built wineries listing page with SSR and SEO metadata
- Updated winery detail page with OpenGraph metadata and "Coming soon" teaser
- Added unit tests for queries and EmptyState component
- Installed nuqs for URL state and @testing-library for component tests
- **QA Improvements (v1.2):**
  - Added WineryCard unit tests (8 tests: link behavior, truncation, image rendering)
  - Added CommuneFilter unit tests (7 tests: URL state updates, selection behavior)
  - Added E2E tests with Playwright for winery directory flows
  - Added LCP performance tests to verify AC12 (<2s budget)
  - Installed @playwright/test and configured playwright.config.ts

### File List

**New Files:**
- `src/server/queries/winery.queries.ts` - Database queries for wineries
- `src/app/(public)/wineries/page.tsx` - Wineries listing page
- `src/components/features/winery/WineryCard.tsx` - Winery card component
- `src/components/features/winery/CommuneFilter.tsx` - Commune filter component
- `src/components/shared/EmptyState.tsx` - Empty state component
- `tests/unit/server/queries/winery.queries.test.ts` - Query unit tests
- `tests/unit/components/shared/EmptyState.test.tsx` - EmptyState component tests
- `tests/unit/components/features/winery/WineryCard.test.tsx` - WineryCard component tests
- `tests/unit/components/features/winery/CommuneFilter.test.tsx` - CommuneFilter component tests
- `tests/e2e/winery-directory.spec.ts` - E2E tests for winery directory
- `tests/e2e/winery-performance.spec.ts` - LCP performance tests
- `tests/setup.ts` - Test setup file
- `playwright.config.ts` - Playwright E2E configuration

**Modified Files:**
- `src/app/layout.tsx` - Added NuqsAdapter provider
- `src/app/(public)/wineries/[slug]/page.tsx` - Added OG metadata, teaser
- `vitest.config.ts` - Added jsdom environment and setup file
- `package.json` - Added nuqs, @testing-library/react, @testing-library/jest-dom, jsdom, @playwright/test

## QA Results

### Review Date: 2026-01-09

### Reviewed By: Quinn (Test Architect)

### Code Quality Assessment

**Overall: GOOD** - Implementation is clean, well-organized, and follows project patterns. All 12 acceptance criteria are implemented. Code demonstrates proper use of Next.js App Router patterns, Server Components for SSR, and appropriate separation of concerns.

**Highlights:**
- Clean query layer separation in `src/server/queries/winery.queries.ts`
- Proper use of Next.js Image component with responsive `sizes` attribute
- URL state management via nuqs for shareable commune filter
- Appropriate error handling with `notFound()` for missing wineries
- Good SEO implementation with static and dynamic metadata

### Refactoring Performed

None required - code quality is sufficient.

### Compliance Check

- Coding Standards: ✓ TypeScript strict mode compliant, no `any` types, proper component structure
- Project Structure: ✓ Files in correct locations per source-tree.md
- Testing Strategy: ✓ Unit tests for queries and EmptyState component
- All ACs Met: ✓ All 12 acceptance criteria implemented

### Requirements Traceability

| AC | Description | Implementation | Test Coverage |
|----|-------------|----------------|---------------|
| 1 | Public `/wineries` page | `(public)/wineries/page.tsx` | ✓ `winery-directory.spec.ts` |
| 2 | Grid with winery cards | `WineryCard.tsx` | ✓ `WineryCard.test.tsx` |
| 3 | Only VERIFIED wineries | `getVerifiedWineries()` | ✓ `winery.queries.test.ts` |
| 4 | Cards link to detail page | `WineryCard.tsx` Link | ✓ `WineryCard.test.tsx` |
| 5 | Detail page content | `[slug]/page.tsx` | ✓ `winery-directory.spec.ts` |
| 6 | Empty state message | `EmptyState` component | ✓ `EmptyState.test.tsx` |
| 7 | SEO metadata | `metadata` exports | ✓ `winery-directory.spec.ts` |
| 8 | Server-side rendering | Server Components | Architectural |
| 9 | Commune filter | `CommuneFilter.tsx` + query | ✓ `CommuneFilter.test.tsx` |
| 10 | Responsive grid | Tailwind classes | ✓ `winery-directory.spec.ts` |
| 11 | "Coming soon" teaser | Card in `[slug]/page.tsx` | ✓ `winery-directory.spec.ts` |
| 12 | LCP < 2s | Image optimization, SSR | ✓ `winery-performance.spec.ts` |

### Improvements Checklist

Items for future consideration (non-blocking):

- [x] Add unit tests for `WineryCard` component (link behavior, truncation)
- [x] Add unit tests for `CommuneFilter` component (URL state updates)
- [x] Add E2E tests for winery listing and detail page flows
- [x] Implement performance budget testing for AC12 (LCP metric)
- [ ] Consider i18n integration when project enables `next-intl`

### Security Review

**Status: PASS** - No security concerns identified.
- Public read-only pages with no user input
- Database queries use Prisma (parameterized, no SQL injection risk)
- No authentication/authorization vulnerabilities (public pages by design)

### Performance Considerations

**Status: PASS** with observation
- Next.js Image component with proper `sizes` and `priority` attributes
- Server-side rendering reduces client-side JS
- Parallel data fetching with `Promise.all()`
- **Observation:** AC12 (LCP < 2s) should be verified with Lighthouse/WebVitals in production

### Files Modified During Review

None - no refactoring performed.

### Gate Status

Gate: **PASS** → docs/qa/gates/1.6-public-winery-directory.yml

### Recommended Status

**✓ Ready for Done** - All acceptance criteria implemented, tests pass, code quality is good. The suggestions in the improvements checklist are enhancements for future iterations, not blockers.
